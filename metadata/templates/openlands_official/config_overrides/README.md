# Openlands Config Overrides

Config overrides keep the relaxed Standard experience separate from optional harder overlays.

- `openlands_standard.json` is the default.
- `openlands_hardlands` may enable stricter systems later.
- No other overlay may silently make the default experience hardcore.

