# Native Templates

Native templates should include an addon descriptor, package metadata, a minimal runtime entrypoint, and validation notes.

Use templates as starting points only; update IDs, versions, dependencies, and runtime targets before release.
