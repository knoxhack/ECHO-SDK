# ECHO Native Addon Template

Scaffolds a public native addon shell with the standard descriptor, AI safety metadata, validation stub, and release checklist. Use it as the default starting point for addon authors who should compile against the Native SDK contracts instead of loader internals or NeoForge runtime APIs.
