# Screen/HUD Example

Legacy bridge/reference scaffold for ScreenCore and HUD surface patterns.

This template ID renders from `_shared/addon-module`, not the Native-first RC1 `.echo-addon` scaffold. Use `echo-native-screen-hud-example` or `new-addon-template` for release-mode Native addon work.
