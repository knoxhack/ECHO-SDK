# Packet Example

Legacy bridge/reference scaffold for packet and network payload patterns.

This template ID renders from `_shared/addon-module`, not the Native-first RC1 `.echo-addon` scaffold. Use `echo-native-packet-example` or `new-addon-template` for release-mode Native addon work.
